---
globs: *.cs
description: POCU C# coding standards and naming conventions
---

# POCU C# 코딩 표준

POCU 아카데미의 C# 코딩 표준을 기반으로 한 규칙입니다.

## 🏷️ 네이밍 컨벤션

### 클래스, 구조체, 메서드

- **파스칼 표기법(PascalCase)** 사용
- 클래스: `PlayerManager`, `CardSystem`
- 구조체: `PlayerData`, `CardInfo` (또는 `SPlayerData` - S 접두사)
- 메서드: `GetPlayerData()`, `UpdateCardStatus()`

```csharp
public class PlayerManager
{
    public void UpdatePlayerStatus() { }
    public bool IsPlayerAlive() { }
}

public struct PlayerData  // 또는 SPlayerData
{
    public int Health;
    public string Name;
}
```

### 지역 변수 및 매개변수

- **카멜 표기법(camelCase)** 사용

```csharp
public void ProcessCard(int cardId, string cardName)
{
    int playerHealth = 100;
    string playerName = "Player1";
    bool isGameActive = true;
}
```

### 상수

- **모든 대문자 + 언더스코어** 사용

```csharp
public class GameConstants
{
    public const int MAX_PLAYER_COUNT = 8;
    public const string DEFAULT_PLAYER_NAME = "Guest";
    public const float MOVEMENT_SPEED = 5.0f;
}
```

### 멤버 변수

- **private 멤버 변수**: `m` 접두사 + 파스칼 표기법

```csharp
public class PlayerController
{
    private int mHealth;
    private string mPlayerName;
    private bool mIsAlive;

    public int Health
    {
        get => mHealth;
        set => mHealth = value;
    }
}
```

### 부울 변수 및 프로퍼티

- **부울 변수**: `b` 접두사 사용
- **부울 프로퍼티/메서드**: `Is`, `Has`, `Can`, `Should` 접두사 사용

```csharp
public class Player
{
    private bool bIsAlive;
    private bool bCanMove;

    public bool IsAlive => bIsAlive;
    public bool HasWeapon { get; set; }
    public bool CanAttack() { return bCanMove && HasWeapon; }
    public bool ShouldRespawn() { return !bIsAlive; }
}
```

### 특수 타입 네이밍

- **인터페이스**: `I` 접두사
- **열거형**: `E` 접두사
- **구조체**: `S` 접두사 (선택사항, readonly struct는 예외)

```csharp
public interface IMovable
{
    void Move(Vector3 direction);
}

public enum EPlayerState
{
    Idle,
    Moving,
    Dead
}

public struct SPlayerStats  // 또는 PlayerStats
{
    public int Strength;
    public int Agility;
}

public readonly struct PlayerPosition  // readonly struct는 S 접두사 제외
{
    public readonly float X;
    public readonly float Y;
}
```

## 📐 코드 포맷팅

### 들여쓰기

- **4칸 스페이스** 사용 (탭 아님)
- 비주얼 스튜디오 기본값 준수

### 중괄호 규칙

- 중괄호 `{`는 **항상 새로운 줄**에서 시작
- 코드가 한 줄이어도 **반드시 중괄호 사용**

```csharp
// 올바른 예시
if (condition)
{
    DoSomething();
}

while (isRunning)
{
    Update();
}

// 잘못된 예시
if (condition) DoSomething();  // 중괄호 없음
if (condition) { DoSomething(); }  // 같은 줄에 중괄호
```

### 변수 선언

- **한 줄에 변수 하나만** 선언

```csharp
// 올바른 예시
int health = 100;
int mana = 50;
string playerName = "Player";

// 잘못된 예시
int health = 100, mana = 50;  // 한 줄에 여러 변수
```

## 🎯 메서드 네이밍 규칙

### 일반 메서드

- **동사(명령형) + 명사(목적어)** 형태

```csharp
public class CardManager
{
    public void AddCard(Card card) { }
    public void RemoveCard(int cardId) { }
    public Card GetCard(int cardId) { return null; }
    public void UpdateCardStatus(int cardId, CardStatus status) { }
}
```

### 부울 반환 메서드

- `Is`, `Can`, `Has`, `Should` 접두사 사용

```csharp
public class GameLogic
{
    public bool IsGameOver() { return false; }
    public bool CanPlayerMove(Player player) { return true; }
    public bool HasRequiredItems(Player player) { return true; }
    public bool ShouldEndTurn() { return false; }
}
```

## 🔧 프로퍼티 사용

### Getter/Setter 대신 프로퍼티 사용

```csharp
public class Player
{
    private int mHealth;

    // 올바른 예시 - 프로퍼티 사용
    public int Health
    {
        get => mHealth;
        set => mHealth = Mathf.Clamp(value, 0, 100);
    }

    // 자동 프로퍼티 (backing field 불필요한 경우)
    public string Name { get; set; }
    public bool IsAlive { get; private set; }
}
```

## 🎮 Unity 특화 적용

### NetworkBehaviour와 POCU 표준 조합

```csharp
public class PlayerNetworkManager : NetworkBehaviour
{
    private int mHealth;
    private bool bIsHost;

    public int Health => mHealth;
    public bool IsHost => bIsHost;

    [ServerRpc]
    public void UpdatePlayerHealthServerRpc(int newHealth)
    {
        if (CanUpdateHealth(newHealth))
        {
            mHealth = newHealth;
        }
    }

    private bool CanUpdateHealth(int health)
    {
        return health >= 0 && health <= MAX_HEALTH;
    }
}
```

### 싱글톤 패턴과 POCU 표준

```csharp
public class GameManager : MonoBehaviour
{
    private static GameManager mInstance;
    private bool bIsInitialized;

    public static GameManager Instance => mInstance;
    public bool IsInitialized => bIsInitialized;

    private void Awake()
    {
        if (mInstance == null)
        {
            mInstance = this;
            DontDestroyOnLoad(gameObject);
            bIsInitialized = true;
        }
        else if (mInstance != this)
        {
            Destroy(gameObject);
        }
    }
}
```

## ⚠️ 추가 규칙

### Switch 문

- 항상 `default` 케이스 포함

```csharp
public void HandlePlayerState(EPlayerState state)
{
    switch (state)
    {
        case EPlayerState.Idle:
            HandleIdle();
            break;
        case EPlayerState.Moving:
            HandleMovement();
            break;
        case EPlayerState.Dead:
            HandleDeath();
            break;
        default:
            Debug.LogError($"Unknown player state: {state}");
            break;
    }
}
```

### 접근 제한자

- 모든 멤버에 명시적으로 접근 제한자 작성

```csharp
public class ExampleClass
{
    private int mPrivateField;
    protected int mProtectedField;
    public int PublicProperty { get; set; }

    private void PrivateMethod() { }
    protected virtual void ProtectedMethod() { }
    public void PublicMethod() { }
}
```

이 표준을 따라 POCU 아카데미의 C# 코딩 표준에 맞는 일관된 코드를 작성하세요.
